{
  "name": "repo",
  "private": true,
  "workspaces": [
    "apps/wms-desktop",
    "packages/*",
    "services/*"
  ],
  "packageManager": "npm@11.6.2",
  "scripts": {
    "build": "npm run -w packages/shared-types build && npm run -w services/core-api build"
  }
}
